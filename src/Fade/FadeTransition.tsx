import { defineComponent, PropType, ref } from 'vue';

const FadeTransition = defineComponent({
    name: 'FadeTransition',
    setup: () => {
        return () => {
            return (<div>saber</div>)
        }
    }
})

export default FadeTransition;
